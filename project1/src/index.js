import React from 'react';
import ReactDOM from 'react-dom';
import NumberManagement from './App';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <NumberManagement />
  </React.StrictMode>,
  document.getElementById('root')
);